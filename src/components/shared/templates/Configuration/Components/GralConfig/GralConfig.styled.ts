import styled from 'styled-components';

export const StyledGralConfig = styled.section`
  width: 1060px;
  height: max-content;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  flex-wrap: wrap;
  margin: 0 auto;
  border-radius: 10px;
  position: relative;
`;
