export interface ICustomWebChat {
  title: string;
  description: string;
  primaryColor: string;
  avatar: string;
  customizeMyAvatar?: boolean;
  secondaryColor: string;
  customIsColor: boolean;
  isAnimation: boolean;
}

export interface IPropsCustom {
  primaryColor: string;
  secondaryColor: string;
  customIsColor: boolean;
  isAnimation: boolean;
}
